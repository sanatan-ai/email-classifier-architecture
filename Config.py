# This file contains variable names used across the overall project.
# For example, column names, file paths, and shared settings.

class Config:
    # Input Columns
    TICKET_SUMMARY = 'Ticket Summary'
    INTERACTION_CONTENT = 'Interaction content'

    # Type Columns to test
    TYPE_COLS = ['y2', 'y3', 'y4']
    CLASS_COL = 'y2'
    GROUPED = 'y1'

    # Data Path — switch dataset
    DATA_PATH = 'data/AppGallery.csv'
    # DATA_PATH = 'data/Purchasing.csv'
